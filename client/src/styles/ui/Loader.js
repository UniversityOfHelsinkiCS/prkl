import makeStyles from '@mui/styles/makeStyles';

const useLoaderStyle = makeStyles({
  root: {
    display: 'flex',
    position: 'relative',
    marginLeft: '50%',
    marginTop: '15%',
  },
});

// eslint-disable-next-line import/prefer-default-export
export { useLoaderStyle };
