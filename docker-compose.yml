version: '3.7'

services:
  app:
    build:
      context: ./
      dockerfile: dev.Dockerfile
    volumes:
      - ./server:/usr/src/app/server
    ports:
      - "3001:3001"
    image: prkl-dev
    container_name: prkl-dev
