{
  "name": "prkl-server",
  "version": "1.0.0-alpha",
  "description": "Backend for PRKL service.",
  "main": "app.js",
  "scripts": {
    "dev": "NODE_ENV=development npx nodemon",
    "build": "npx tsc",
    "start": "NODE_ENV=production npx tsc && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.2",
    "@types/node": "^13.1.7",
    "ts-node": "^8.6.2",
    "tslint": "^5.20.1",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
